{
  "name": "gas_calculator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "run-pty % elm-watch hot % npm run esbuild -- --serve=9000 --servedir=dist",
    "build": "elm-watch make --optimize && npm run esbuild -- --minify",
    "esbuild": "esbuild app.ts --bundle --outdir=dist/ "
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "elm": "^0.19.1-6"
  },
  "dependencies": {
    "elm-watch": "^1.1.2",
    "esbuild": "^0.19.11",
    "run-pty": "^4.1.0"
  }
}